백업 종류
전체 백업
	-특정 시점의 데이터 파일 전체를 백업
차등 백업
	-마지막 전체 백업 이후에 변경된 데이터만 백업 
트랜잭션 로그 백업
	-마지막 로그 백업 이후 쌓인 트랜잭션 로그(ldf)를 백업하고, 로그를 비워냄

Q. 차등 백업과 트랜잭션 로그 백업의 차이
	차등 백업 : 전체 백업 이후 시점까지의 내역을 통째로 다시 기록
	로그 백업 : 마지막 백업 시점을 기준으로 초단위 복구 가능 (느림)
복구 모델의 종류
SIMPLE : 로그 백업이 불가능, 체크포인트 발생 시 로그 자동 삭제
FULL : 모든 트랜잭션 로그를 보존. 로그 백업 필수
BULK-Logged : 대량 작업에 대한 로그 기록을 최소화

*Log Sequence Number과 로그 체인
DB의 모든 변경 사항은 고유 번호인 LSN이 매겨져 순차적으로 기록됨
로그 체인 : LSN들이 끊어지지 않고 쭉 연결된 상태
Full Backup을 하면 현재 LSN이 기록됨

Log Backup 1 : FULL Backup 이후의 LSN 기록
Log Backup 2 : Log Backup 1 이후의 LSN 기록

SIMPLE :
LSN 100~200 기록됨 -> Checkpoint 발생 -> LSN 100~200 삭제됨(덮어씀).
LSN 201~300 기록됨 -> 삭제됨.
상태: 과거의 연결 고리가 계속 끊어지면서 현재만 존재함.

FULL 복구 모델에서는 체크포인트가 발생해도 로그가 지워지지 않음.
오직 "트랜잭션 로그 백업"을 수행했을 때만 비워짐 (TRUNCATE)
전체 백업 주기가 1주이고 트랜잭션 로그 백업 주기가 12시간일 경우에는 어떤식으로 동작하는지?

전체 백업시 mdf 전체 백업함 (복구 기준점이 됨 Base LSN)
-> 사용자의 변경 사항이 트랜잭션 로그 파일(ldf)에 계속 쌓임
-> 12시간 동안의 변경 기록이 trn으로 저장됨 -> ldf 파일의 내부 공간이 비워짐

